// Placeholder file remained under __tests__; add a trivial test to avoid empty suite failures.
// Note: real test utilities have been moved to `components/Categorias/Gastos/test-utils.tsx`.

test('placeholder test-utils passes', () => {
  expect(true).toBe(true);
});
